// {{COMPONENT_NAME}} — {{COMPONENT_DESCRIPTION}}
//
// Dumb component: props in, JSX out. No data fetching. No services. No
// state that matters. References theme tokens, never literal values.

{{MEMO_IMPORT}}import { Card, Text } from "react-native-paper";

import { colors, spacing } from "@/theme";

import type { {{COMPONENT_NAME}}Props } from "./{{COMPONENT_KEBAB}}.interface";

export const {{COMPONENT_NAME}} = {{MEMO_OPEN}}({ {{PROP_NAMES}} }: {{COMPONENT_NAME}}Props) => (
  <Card mode="contained" style={{ margin: spacing.md, padding: spacing.md }}>
    <Card.Content>
      <Text variant="titleMedium" style={{ color: colors.textPrimary }}>
        {/* TODO: render the props above using Paper components and theme tokens */}
      </Text>
    </Card.Content>
  </Card>
){{MEMO_CLOSE}};
