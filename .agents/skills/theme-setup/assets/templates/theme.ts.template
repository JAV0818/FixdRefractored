// Composed theme. Generated by theme-setup.
//
// Maps our semantic tokens to React Native Paper's MD3Theme shape. Components
// should pull from useTheme() rather than importing tokens directly — this
// makes dark/light switching automatic.

import { MD3DarkTheme, MD3LightTheme, configureFonts } from "react-native-paper";
import type { MD3Theme } from "react-native-paper";

import { lightColors, darkColors } from "./colors";
import { fontFamily, fontSize, fontWeight, lineHeight, letterSpacing } from "./typography";

const baseFontConfig = {
  fontFamily: fontFamily.body,
  fontWeight: fontWeight.regular,
  letterSpacing: letterSpacing.normal,
} as const;

const fontConfig = {
  displayLarge: { ...baseFontConfig, fontFamily: fontFamily.display, fontWeight: fontWeight.bold, fontSize: fontSize["6xl"], lineHeight: fontSize["6xl"] * lineHeight.tight },
  displayMedium: { ...baseFontConfig, fontFamily: fontFamily.display, fontWeight: fontWeight.bold, fontSize: fontSize["5xl"], lineHeight: fontSize["5xl"] * lineHeight.tight },
  displaySmall: { ...baseFontConfig, fontFamily: fontFamily.display, fontWeight: fontWeight.bold, fontSize: fontSize["4xl"], lineHeight: fontSize["4xl"] * lineHeight.tight },
  headlineLarge: { ...baseFontConfig, fontFamily: fontFamily.display, fontWeight: fontWeight.semibold, fontSize: fontSize["3xl"], lineHeight: fontSize["3xl"] * lineHeight.tight },
  headlineMedium: { ...baseFontConfig, fontFamily: fontFamily.display, fontWeight: fontWeight.semibold, fontSize: fontSize["2xl"], lineHeight: fontSize["2xl"] * lineHeight.tight },
  headlineSmall: { ...baseFontConfig, fontFamily: fontFamily.display, fontWeight: fontWeight.semibold, fontSize: fontSize.xl, lineHeight: fontSize.xl * lineHeight.tight },
  titleLarge: { ...baseFontConfig, fontWeight: fontWeight.semibold, fontSize: fontSize.lg, lineHeight: fontSize.lg * lineHeight.normal },
  titleMedium: { ...baseFontConfig, fontWeight: fontWeight.medium, fontSize: fontSize.base, lineHeight: fontSize.base * lineHeight.normal },
  titleSmall: { ...baseFontConfig, fontWeight: fontWeight.medium, fontSize: fontSize.sm, lineHeight: fontSize.sm * lineHeight.normal },
  bodyLarge: { ...baseFontConfig, fontSize: fontSize.lg, lineHeight: fontSize.lg * lineHeight.normal },
  bodyMedium: { ...baseFontConfig, fontSize: fontSize.base, lineHeight: fontSize.base * lineHeight.normal },
  bodySmall: { ...baseFontConfig, fontSize: fontSize.sm, lineHeight: fontSize.sm * lineHeight.normal },
  labelLarge: { ...baseFontConfig, fontWeight: fontWeight.medium, fontSize: fontSize.sm, lineHeight: fontSize.sm * lineHeight.normal, letterSpacing: letterSpacing.wide },
  labelMedium: { ...baseFontConfig, fontWeight: fontWeight.medium, fontSize: fontSize.xs, lineHeight: fontSize.xs * lineHeight.normal, letterSpacing: letterSpacing.wide },
  labelSmall: { ...baseFontConfig, fontWeight: fontWeight.medium, fontSize: 11, lineHeight: 11 * lineHeight.normal, letterSpacing: letterSpacing.wider },
};

export const lightTheme: MD3Theme = {
  ...MD3LightTheme,
  colors: {
    ...MD3LightTheme.colors,
    primary: lightColors.primary,
    onPrimary: lightColors.background,
    primaryContainer: lightColors.surfaceVariant,
    onPrimaryContainer: lightColors.textPrimary,
    secondary: lightColors.secondary,
    onSecondary: lightColors.background,
    tertiary: lightColors.accent,
    onTertiary: lightColors.textPrimary,
    background: lightColors.background,
    onBackground: lightColors.textPrimary,
    surface: lightColors.surface,
    onSurface: lightColors.textPrimary,
    surfaceVariant: lightColors.surfaceVariant,
    onSurfaceVariant: lightColors.textSecondary,
    outline: lightColors.outline,
    error: lightColors.danger,
    onError: lightColors.background,
  },
  fonts: configureFonts({ config: fontConfig }),
};

export const darkTheme: MD3Theme = {
  ...MD3DarkTheme,
  dark: true,
  colors: {
    ...MD3DarkTheme.colors,
    primary: darkColors.primary,
    onPrimary: darkColors.background,
    primaryContainer: darkColors.surfaceVariant,
    onPrimaryContainer: darkColors.textPrimary,
    secondary: darkColors.secondary,
    onSecondary: darkColors.background,
    tertiary: darkColors.accent,
    onTertiary: darkColors.textPrimary,
    background: darkColors.background,
    onBackground: darkColors.textPrimary,
    surface: darkColors.surface,
    onSurface: darkColors.textPrimary,
    surfaceVariant: darkColors.surfaceVariant,
    onSurfaceVariant: darkColors.textSecondary,
    outline: darkColors.outline,
    error: darkColors.danger,
    onError: darkColors.background,
  },
  fonts: configureFonts({ config: fontConfig }),
};

export const appTheme = {{DEFAULT_THEME}};
