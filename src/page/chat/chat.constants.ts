// Copy for the shared 1:1 chat screen (customer + provider).

export const CHAT_COPY = {
  loading: "Loading messages…",
  error: "We couldn't load these messages.",
  retry: "Try again",
  emptyTitle: "No messages yet",
  emptyBody: "Send a message to get the conversation started.",
  orderSnippetLabel: "Order",
  headerPlaceholder: "Chat",
} as const;
